<form method="post" action="{{ path('appointments_delete', {'id': appointment.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ appointment.id) }}">
	<button class="btn">Effacer</button>
</form>
